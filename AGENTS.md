# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

본 프로젝트의 규칙·spec·명령어는 모두 `CLAUDE.md` 와 동일합니다. Codex 도 동일한 진입점을 따르되, `.claude/` 와 `.codex/` 는 같은 프로젝트 규칙을 담는 미러로 유지하세요.

## Project Rules & Memory

Rules: `.claude/rules/` 와 `.codex/rules/` 디렉토리
Specs: `docs/development/fsd-architecture/`

## Workflow Notes

- `.claude/` 와 `.codex/` 가 agent / skill / rule / hook 의 진실 소스
- 프로젝트 명령어 / Conventions / Key Specs → `CLAUDE.md` 참조
